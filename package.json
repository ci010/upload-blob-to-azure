{
  "author": {
    "name": "ci010",
    "email": "cijhn@hotmail.com"
  },
  "bundleDependencies": false,
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@azure/storage-blob": "^12.8.0"
  },
  "deprecated": false,
  "description": "Github action to upload blob to azure storage",
  "devDependencies": {
    "@types/node": "~18",
    "esbuild": "^0.19.10",
    "typescript": "^5.3.3"
  },
  "license": "MIT",
  "main": "dist/index.js",
  "name": "upload-blob-to-azure",
  "scripts": {
    "build": "esbuild --bundle --platform=node --target=es2020 --outdir=dist --format=cjs --keep-names src/index.ts"
  },
  "version": "0.0.3"
}
